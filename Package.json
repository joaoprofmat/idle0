{
  "name": "idle-game",
  "private": true,
  "version": "0.1.0",
  "homepage": "https://yourusername.github.io/idle-game",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}

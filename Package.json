{
  "name": "idle0",
  "private": true,
  "version": "0.1.0",
  "homepage": "https://joaoprofmat.github.io/idle0",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}
